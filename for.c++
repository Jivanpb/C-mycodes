#include<iostream>
using namespace std;
int main()
{
    int p;
    string str1[1000]={'red','blue','orange','grey','saffron','pink','red','saffron','orange'};
    //for(arr=0;arr<0;arr++)
    {
        // for(p=0;p<array.length;p++)
        // cout<<"the dupticate of colors in j is "<<j<<endl;
        // p++;
        cout<<"the colors in str1 is "<<str1<<endl;
    }
    return  0;
}